# 架构说明

## 设计目标

远行簿是一个部署在 GitHub Pages 上的公开旅行计划网站，采用纯静态架构：不需要构建工具、后端服务、数据库或运行时环境。

总站只负责按时间展示旅行计划和提供入口。每个旅行计划是独立的静态子站，可以保留自己的布局、样式、脚本和数据结构，不要求套用统一模板。

## 页面分层

```text
vacation/
├── index.html                    # 总站首页
├── trip.html                     # 旧链接兼容跳转页
├── assets/
│   ├── home.js                   # 总站时间线渲染逻辑
│   ├── trips.js                  # 总站旅行计划元数据
│   ├── site.css                  # 总站样式
│   └── images/                   # 总站封面图
├── trips/
│   ├── jiangnan-2026/            # 江南计划独立子站
│   │   ├── index.html
│   │   ├── style.css
│   │   ├── data.js
│   │   ├── main.js
│   │   └── assets/images/        # 江南计划自己的图片
│   └── yunnan-2026/              # 云南计划独立子站
│       ├── index.html
│       ├── data.js
│       ├── app.js
│       ├── 其他页面模块
│       └── assets/images/        # 云南计划自己的图片
└── .github/workflows/pages.yml   # 内容扫描与 GitHub Pages 部署
```

## 路由关系

总站地址为仓库对应的 GitHub Pages 根路径，例如：

```text
https://<user>.github.io/vacation/
```

子页面使用目录式地址：

```text
/trips/jiangnan-2026/
/trips/yunnan-2026/
```

子页面通过相对链接 `../../` 返回总站，因此本地静态服务器、GitHub Pages 项目站点以及未来更换域名时都不需要硬编码域名或仓库名。

历史链接 `/trip.html?id=<trip-id>` 由根目录的 `trip.html` 映射到对应子页面。这个文件只用于向后兼容，不承载旅行内容；新增计划时如需兼容同类旧链接，也要同步增加映射。

## 数据流

总站的计划列表以 `assets/trips.js` 中导出的 `trips` 数组为唯一入口。`assets/home.js` 读取该数组，完成以下工作：

- 按出发时间生成时间线
- 生成年份筛选项
- 渲染封面、日期、路线和摘要
- 使用每条记录的 `page` 字段跳转到独立子站

各子站的数据和交互由自己的文件管理。总站不会读取子站内部数据，子站也不依赖总站脚本，因此修改某个计划的页面设计不会影响其他计划。

## 新增旅行计划

1. 在 `trips/` 下创建一个语义清晰且稳定的目录，例如 `trips/japan-2027/`。
2. 在该目录中放置可独立运行的 `index.html`、样式、脚本和数据；该计划的图片放在自己的 `assets/images/` 下。
3. 在子页面显眼位置加入指向 `../../` 的“返回总站”链接。
4. 在 `assets/trips.js` 的 `trips` 数组中添加总站卡片元数据，并将 `page` 设置为 `./trips/japan-2027/`。
5. 如需支持旧式查询参数链接，在 `trip.html` 的 `pages` 映射中添加相同 ID。
6. 使用本地静态服务器检查总站、子页面、图片、交互和返回链接。
7. 提交前检查所有内容都适合公开发布。

## 部署流程

推送到 `main` 分支后，`.github/workflows/pages.yml` 会自动执行：

1. 检出仓库。
2. 扫描 HTML、CSS、JavaScript 和 JSON 文件中的内部域名、密钥、令牌及私钥特征。
3. 扫描通过后，将整个仓库作为静态站点制品上传。
4. 发布到 GitHub Pages。

项目没有编译产物，仓库中的页面和资源就是最终发布内容。修改后可直接用静态服务器预览：

```bash
python3 -m http.server 8000
```

## 公开内容边界

允许发布公开路线、城市、概略日期、景点、公共交通方式、住宿区域、美食、预算范围和通用出行提示。

禁止提交账号、密钥、令牌、内部域名、内部文档、个人身份信息、精确住址、订单号、座位号、酒店房号及其他私密预订信息。自动扫描只能覆盖常见特征，提交者仍需在发布前人工检查文字、图片元数据和资源来源。
